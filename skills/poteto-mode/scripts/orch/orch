#!/usr/bin/env bun
import { ensureDependenciesInstalled } from "../bootstrap.ts";

ensureDependenciesInstalled();
const { main } = await import("./orch.ts");
process.exitCode = await main(process.argv.slice(2));
